
export interface GridDimensions {
  width: number;
  height: number;
}

// Placeholder for potential API error structure from a real backend
export interface ApiError {
  message: string;
  statusCode?: number;
}
